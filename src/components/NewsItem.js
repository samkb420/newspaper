import React from 'react'

function NewsItem() {
    return (
        <div className="">
            <div className="card-body">
                <p>React App doesn’t handle backend logic or databases; it just creates a frontend build pipeline, so you can use it with any backend you want. Under the hood, it uses Babel and webpack</p>
            </div>
        </div>
    )
}

export default NewsItem
